language: java

dist: trusty

jdk:
  - oraclejdk8

install: true

notifications:
  email:
    recipients:
      - stream-registry-committers@groups.homeawaycorp.com
    on_success: change
    on_failure: always

cache:
  directories:
  - "$HOME/.m2/repository"